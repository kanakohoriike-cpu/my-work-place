# CLAUDE.md - マルチエージェント構成

## プロジェクト概要

このリポジトリは、Claude Code 向けのマルチエージェント構成を定義するプロジェクトである。
メインエージェント（Opus）がタスクを統括し、専門サブエージェント（Sonnet / Haiku）に作業を委譲する構成を採用する。

---

## リポジトリ構成

```
my-work-place/
├── CLAUDE.md                    # このファイル。AI アシスタント向けの指針
└── .claude/
    └── agents/                  # サブエージェント定義ファイル
        ├── researcher.md        # 調査専門エージェント（Haiku）
        ├── implementer.md       # 実装専門エージェント（Sonnet）
        ├── reviewer.md          # レビュー専門エージェント（Sonnet）
        └── docs-writer.md       # ドキュメント専門エージェント（Haiku）
```

### ファイル説明

| ファイル | 役割 |
|---|---|
| `CLAUDE.md` | プロジェクト全体の方針・規約・ワークフローを定義 |
| `.claude/agents/researcher.md` | コード調査・ファイル検索・情報収集エージェントの定義 |
| `.claude/agents/implementer.md` | コード実装・ファイル編集エージェントの定義 |
| `.claude/agents/reviewer.md` | コードレビュー・品質チェックエージェントの定義 |
| `.claude/agents/docs-writer.md` | ドキュメント・コメント生成エージェントの定義 |

---

## エージェント構成

### メインエージェント（オーケストレーター）

- **モデル**: `claude-opus-4-6`（最新の最高性能モデル）
- **役割**: タスク分解、サブエージェントへの委譲、最終統合、品質判断
- **方針**:
  - 複雑な判断・設計・レビューはメインエージェントが担当する
  - 単純な調査・検索・定型処理はサブエージェントに委譲する
  - サブエージェントの出力を検証してから最終回答に組み込む

### サブエージェント一覧

| エージェント名 | モデル | 用途 | 利用可能ツール |
|---|---|---|---|
| `researcher` | haiku | コード調査・ファイル検索・情報収集 | Read, Grep, Glob, Bash |
| `implementer` | sonnet | コード実装・ファイル編集・新規作成 | Read, Write, Edit, Bash, Glob, Grep |
| `reviewer` | sonnet | コードレビュー・品質チェック・セキュリティ確認 | Read, Grep, Glob, Bash |
| `docs-writer` | haiku | ドキュメント・コメント・README 生成 | Read, Write, Edit, Glob, Grep |

### サブエージェント共通ルール

- **出力上限: 2000文字以内**（厳守）
- 冗長な説明は不要。結果と要点のみ返す
- 完了時は構造化された要約を返す
- 不明点がある場合はその旨だけ報告し、勝手に推測しない

---

## タスク委譲ガイドライン

### サブエージェントに委譲すべきタスク

- ファイルの探索・grep・パターン検索 → `researcher`
- 定型的なコード変更（リネーム、フォーマット修正等） → `implementer`
- テストの実行と結果収集 → `implementer` または `researcher`
- ドキュメント・コメントの下書き → `docs-writer`
- 単純なバグ修正 → `implementer`
- コード変更後の品質チェック → `reviewer`

### メインエージェントが直接行うべきタスク

- アーキテクチャ設計・方針決定
- 複数ファイルにまたがる複雑なリファクタリング
- セキュリティ判断・機密情報を含む処理
- サブエージェントの出力の最終レビューと統合
- ユーザーへの最終回答の組み立て

---

## ワークフロー例

### 標準フロー

```
ユーザーの依頼
    ↓
[メイン(Opus)] タスク分析・分解
    ↓
[researcher(Haiku)] 関連コード調査 → 要約を返す
    ↓
[メイン(Opus)] 調査結果を評価、実装方針を決定
    ↓
[implementer(Sonnet)] 実装を実行 → 変更内容を返す
    ↓
[reviewer(Sonnet)] 変更をレビュー → 指摘事項を返す
    ↓
[メイン(Opus)] 最終統合・ユーザーへ回答
```

### 調査のみのフロー

```
ユーザーの質問
    ↓
[researcher(Haiku)] コードベース調査
    ↓
[メイン(Opus)] 調査結果をもとに回答を組み立て
```

### ドキュメント作成フロー

```
ドキュメント作成依頼
    ↓
[researcher(Haiku)] 対象コードの調査
    ↓
[docs-writer(Haiku)] ドキュメント作成
    ↓
[メイン(Opus)] 内容確認・必要なら修正指示
```

---

## サブエージェント定義の書き方

`.claude/agents/` ディレクトリに Markdown ファイルを配置する。
ファイル先頭に YAML Frontmatter でメタデータを指定する。

```markdown
---
name: agent-name
description: エージェントの用途説明（自動起動トリガーにも使われる）
tools: Read, Write, Edit, Bash, Glob, Grep
model: haiku | sonnet | opus
---
エージェントへのシステムプロンプト（指示内容）
```

### Frontmatter フィールド

| フィールド | 必須 | 説明 |
|---|---|---|
| `name` | 必須 | エージェントの識別名（ファイル名と一致推奨） |
| `description` | 必須 | 用途の説明。Task ツールの自動選択に使われる |
| `tools` | 任意 | 使用可能なツール一覧（カンマ区切り） |
| `model` | 任意 | 使用するモデル（省略時は親エージェントを継承） |

---

## Git ワークフロー

### ブランチ命名規則

- 機能ブランチ: `claude/<目的>-<セッションID>`
- 例: `claude/claude-md-mm0khbr1s7p88a85-9RdRn`

### コミットメッセージ規則

- 日本語または英語で簡潔に記述
- 変更の「なぜ」を中心に書く
- 例: `Add sub-agent definition files for multi-agent setup`

### プッシュ手順

```bash
# ブランチをリモートに push
git push -u origin <branch-name>

# ネットワークエラー時はエクスポネンシャルバックオフで最大4回リトライ
```

---

## コーディング規約

- **言語**: 指定がなければ TypeScript / Python を優先
- **コメント**: 日本語で記述
- **エラーハンドリング**: 必ず明示的に行う
- **テスト**: 変更には対応するテストを含める
- **スコープ**: 指示された変更のみ行い、無断の「改善」はしない
- **セキュリティ**: ハードコードされた秘密情報・インジェクション等を避ける

---

## レビュー観点

`reviewer` エージェントが確認する項目:

1. **バグ・ロジックエラー**: 意図しない動作や境界値処理の漏れ
2. **セキュリティリスク**: ハードコードされた秘密情報、インジェクション脆弱性
3. **エラーハンドリング**: 例外処理・エラー伝播の適切性
4. **パフォーマンス**: 不要なループ・メモリリーク・N+1問題等
5. **命名・可読性**: 変数名・関数名の明確さ

---

## 注意事項

- 各サブエージェントは **2000文字以内** で出力する（厳守）
- `researcher` と `reviewer` は **読み取り専用**（ファイル編集不可）
- `implementer` は **指示スコープ外の変更禁止**（勝手な改善をしない）
- `docs-writer` は **コード本体の変更禁止**（ドキュメント・コメントのみ）
