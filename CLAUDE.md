# CLAUDE.md - マルチエージェント構成

## プロジェクト概要

このプロジェクトでは、メインエージェント（Opus）がタスクを統括し、
専門サブエージェント（Sonnet / Haiku）に作業を委譲するマルチエージェント構成を採用する。

---

## エージェント構成

### メインエージェント（オーケストレーター）

- **モデル**: `opus`（最新の最高性能モデル）
- **役割**: タスク分解、サブエージェントへの委譲、最終統合、品質判断
- **方針**:
  - 複雑な判断・設計・レビューはメインエージェントが担当する
  - 単純な調査・検索・定型処理はサブエージェントに委譲する
  - サブエージェントの出力を検証してから最終回答に組み込む

### サブエージェント一覧

| エージェント名 | モデル | 用途 |
|---|---|---|
| `researcher` | haiku | コード調査・ファイル検索・情報収集 |
| `implementer` | sonnet | コード実装・ファイル編集 |
| `reviewer` | sonnet | コードレビュー・品質チェック |
| `docs-writer` | haiku | ドキュメント・コメント生成 |

### サブエージェント共通ルール

- **出力上限: 2000文字以内**（厳守）
- 冗長な説明は不要。結果と要点のみ返す
- 完了時は構造化された要約を返す
- 不明点がある場合はその旨だけ報告し、勝手に推測しない

---

## タスク委譲ガイドライン

### サブエージェントに委譲すべきタスク

- ファイルの探索・grep・パターン検索
- 定型的なコード変更（リネーム、フォーマット修正等）
- テストの実行と結果収集
- ドキュメント・コメントの下書き
- 単純なバグ修正

### メインエージェントが直接行うべきタスク

- アーキテクチャ設計・方針決定
- 複数ファイルにまたがる複雑なリファクタリング
- セキュリティ判断・機密情報を含む処理
- サブエージェントの出力の最終レビューと統合
- ユーザーへの最終回答の組み立て

---

## ワークフロー例

```
ユーザーの依頼
    ↓
[メイン(Opus)] タスク分析・分解
    ↓
[researcher(Haiku)] 関連コード調査 → 要約を返す
    ↓
[メイン(Opus)] 調査結果を評価、実装方針を決定
    ↓
[implementer(Sonnet)] 実装を実行 → 変更内容を返す
    ↓
[reviewer(Sonnet)] 変更をレビュー → 指摘事項を返す
    ↓
[メイン(Opus)] 最終統合・ユーザーへ回答
```

---

## コーディング規約

- 言語: 指定がなければ TypeScript / Python を優先
- コメント: 日本語で記述
- エラーハンドリング: 必ず明示的に行う
- テスト: 変更には対応するテストを含める

---

## サブエージェント定義ファイル

サブエージェントは `.claude/agents/` ディレクトリに配置する。
詳細は各 `.md` ファイルを参照。
